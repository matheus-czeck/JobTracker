import { PipesReplaceUnderscorePipe } from './pipes.replace-underscore.pipe';

describe('PipesReplaceUnderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new PipesReplaceUnderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
